# Development dependencies for Q-Empire Social Autopilot
# Install: pip install -r requirements-dev.txt

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
pytest-mock>=3.14.0

# Linting & Formatting
ruff>=0.4.0
mypy>=1.10.0
isort>=5.13.0

# Pre-commit hooks
pre-commit>=3.7.0

# Type stubs for common packages
# (add stubs as needed: types-requests, types-pyyaml, etc.)
